@page
@model Cde.Pages.Projects.ProjectsModel
@{
    ViewData["Title"] = "Projects";
}
<h1>@ViewData["Title"]</h1>

<p>Projects</p>

<a asp-page="Create">Create</a>

@foreach (var i in Model.Projects)
{
    <li>
        <a asp-page="View" asp-route-id="@i.ProjectId">@i</a>
    </li>
}